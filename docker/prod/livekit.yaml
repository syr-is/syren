port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true
keys:
  # Override via LIVEKIT_KEYS env var at runtime
  # Format:  api_key: api_secret
  devkey: devsecret
logging:
  level: info
